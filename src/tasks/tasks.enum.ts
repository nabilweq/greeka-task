export enum TaskStatus {
    PENDING = 'pending',
    DONE = 'done',
    IN_PROGRESS = 'in_progress',
    PAUSED = 'paused',
}

export enum TaskPriority {
    HIGH = 'high',
    MEDIUM = 'medium',
    NORMAL = 'normal',
}